[package]
name = "tank_attack_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
sfml = { version = "0.25.1", features = ["graphics", "window", "audio"] }
rand = "0.10.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
